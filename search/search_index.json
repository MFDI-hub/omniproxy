{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"for_mine_and_only_my/","title":"AsyncProxyPool \u2014 step-by-step walkthrough","text":"<p>Follow the proxy pool logic in order, or jump to any section via the table of contents. All line references point at <code>omniproxy/pool.py</code>.</p>"},{"location":"for_mine_and_only_my/#table-of-contents","title":"Table of contents","text":"<ol> <li>Mental model</li> <li>Usage lifecycle</li> <li>Internal state (what each field tracks)</li> <li>Startup &amp; shutdown</li> <li>Acquire \u2014 the main loop</li> <li>After acquire \u2014 release / success / failure</li> <li>Selection &amp; filtering pipeline</li> <li>Background workers</li> <li>Refresh &amp; merge</li> <li>Health checks &amp; dead letter</li> <li>Method index by role</li> <li>External modules</li> <li>Exceptions you can hit</li> </ol>"},{"location":"for_mine_and_only_my/#1-mental-model","title":"1. Mental model","text":"<p><code>AsyncProxyPool</code> is a lease manager over a <code>deque[Proxy]</code>:</p> Concept Meaning Proxy in deque Candidate in the pool (may be cooling down or at cap) Lease <code>_connections[url]</code> counter incremented on <code>acquire</code>, decremented on <code>release</code> / <code>mark_*</code> Eligible Passes filters, not in cooldown, under connection cap Strategy Picks one proxy from eligible list (round-robin, random, weighted, lowest-latency) Circuit breaker Pool-wide gate; when open, <code>acquire</code> waits or raises Sticky session <code>session_key</code> binds to one proxy URL until TTL or rebind <pre><code>flowchart TB\n    subgraph public_api[\"Public API\"]\n        A[acquire]\n        R[release]\n        MS[mark_success]\n        MF[mark_failed]\n    end\n\n    subgraph core_state[\"_state_lock protects\"]\n        Q[\"_proxies deque\"]\n        C[\"_connections\"]\n        CD[\"_cooldown_until\"]\n        SC[\"_scores\"]\n        SR[\"_session_registry\"]\n    end\n\n    subgraph policies[\"Policies\"]\n        CB[CircuitBreaker]\n        ST[SelectionStrategy]\n        FIL[Filters + session]\n    end\n\n    subgraph background[\"Background\"]\n        HC[_health_check_loop]\n        RF[_refresh_loop]\n        DL[_dead_letter_retrier]\n        MT[_metrics_worker]\n    end\n\n    A --&gt; FIL --&gt; ST --&gt; Q\n    A --&gt; CB\n    A --&gt; C\n    R --&gt; C\n    MS --&gt; C\n    MS --&gt; SC\n    MS --&gt; CB\n    MF --&gt; C\n    MF --&gt; SC\n    MF --&gt; CD\n    MF --&gt; CB\n    HC --&gt; Q\n    RF --&gt; Q\n</code></pre> <p>SyncProxyPool (<code>pool.py</code> L1663+) is a thin wrapper: daemon thread + event loop, delegates every call to <code>AsyncProxyPool</code>.</p>"},{"location":"for_mine_and_only_my/#2-usage-lifecycle","title":"2. Usage lifecycle","text":"<pre><code>sequenceDiagram\n    participant App\n    participant Pool as AsyncProxyPool\n    participant BG as Background tasks\n\n    App-&gt;&gt;Pool: async with pool (__aenter__)\n    Pool-&gt;&gt;Pool: _start()\n    Pool-&gt;&gt;BG: health / refresh / dead-letter / metrics\n    Pool-&gt;&gt;Pool: optional warmup\n    Pool-&gt;&gt;Pool: _ready.set()\n\n    App-&gt;&gt;Pool: acquire(filters)\n    Pool--&gt;&gt;App: Proxy\n\n    alt success\n        App-&gt;&gt;Pool: mark_success OR release\n    else failure\n        App-&gt;&gt;Pool: mark_failed(proxy, exc)\n    end\n\n    App-&gt;&gt;Pool: __aexit__ then _close()\n    Pool-&gt;&gt;Pool: drain leases, cancel BG tasks\n</code></pre> <p>Rule: Call exactly one of <code>release</code>, <code>mark_success</code>, or <code>mark_failed</code> per acquisition \u2014 not <code>release</code> + <code>mark_success</code>.</p>"},{"location":"for_mine_and_only_my/#3-internal-state","title":"3. Internal state","text":"<p>Built in <code>__init__</code> (L157\u2013221):</p> Field Type Purpose <code>_proxies</code> <code>deque[Proxy]</code> Ordered pool; front evicted when <code>max_size</code> exceeded <code>_connections</code> <code>dict[str, int]</code> Active leases per proxy URL <code>_cooldown_until</code> <code>dict[str, float]</code> Monotonic timestamp when cooldown ends <code>_scores</code> <code>dict[str, EMAState]</code> EMA success/latency for weighted / lowest-latency strategies <code>_consecutive_failures</code> <code>dict[str, int]</code> Drives cooldown threshold <code>_session_registry</code> <code>dict[str, SessionEntry]</code> Sticky session \u2192 proxy URL + expiry <code>_pending_session_rebind</code> <code>dict[str, Proxy]</code> Old proxy waiting for <code>on_session_rebind</code> hook <code>_circuit_breaker</code> <code>CircuitBreaker \\| None</code> Pool-wide failure shedding <code>_half_open_probe_*</code> epoch, url, proxy Tracks HALF_OPEN probe acquisition <code>_strategy</code> <code>SelectionStrategy</code> Built from <code>config.strategy</code> <code>_dead_letter_queue</code> <code>list</code> Proxies awaiting retry (see <code>dead_letter.py</code>) <code>_refresh_needed</code> <code>bool</code> Urgent refresh when below <code>min_size</code> <code>_refresh_generation</code> <code>int</code> Coalesces concurrent on-demand refreshes <code>_statistics</code> <code>PoolStatistics</code> served / failed / released / exhausted_count <code>_ready</code> <code>Event</code> Blocks <code>acquire</code> until startup completes <code>_draining</code> <code>Event</code> Set during shutdown <code>_state_lock</code> + <code>_available_cond</code> <code>Lock</code> + <code>Condition</code> Serialize state; wake waiters <code>_refresh_lock</code> <code>Lock</code> One refresh at a time <code>_close_lock</code> <code>Lock</code> Idempotent close <p>Locks: always mutate pool state under <code>_state_lock</code>. Refresh uses <code>_refresh_lock</code> then takes <code>_state_lock</code> inside <code>_merge_new_proxies</code>.</p>"},{"location":"for_mine_and_only_my/#4-startup-shutdown","title":"4. Startup &amp; shutdown","text":""},{"location":"for_mine_and_only_my/#startup-chain","title":"Startup chain","text":"<pre><code>__aenter__  \u2192  _start  \u2192  spawn background tasks  \u2192  optional warmup  \u2192  _ready.set()\n</code></pre> Step Method Lines What happens 1 <code>_start</code> L256 Guard closed / already ready 2 <code>_stop_background_tasks</code> L335 Cancel any stale tasks 3 Create tasks L280\u2013301 <code>_health_check_loop</code>, <code>_dead_letter_retrier</code>, <code>_refresh_loop</code>, <code>_metrics_worker</code> \u2014 each optional via config 4 Warmup L303\u2013329 <code>run_warmup</code> + hooks; may <code>raise WarmupFailedError</code> 5 Ready L330 <code>_ready.set()</code> \u2014 unblocks <code>acquire</code> <p>On any startup exception: <code>_close()</code> then re-raise.</p>"},{"location":"for_mine_and_only_my/#shutdown-chain","title":"Shutdown chain","text":"<pre><code>__aexit__ / close  \u2192  _close  \u2192  drain  \u2192  _closed=True  \u2192  _stop_background_tasks\n</code></pre> Step Method Lines What happens 1 <code>_draining.set()</code> L379 New acquires get <code>PoolDrainingError</code> 2 Wait for leases L392\u2013402 Until <code>_connections</code> sum is 0 or <code>drain_timeout</code> 3 <code>_closed = True</code> L404 Permanent 4 Cancel BG tasks L408 Health, refresh, metrics, dead-letter"},{"location":"for_mine_and_only_my/#5-acquire-the-main-loop","title":"5. Acquire \u2014 the main loop","text":"<p>Entry: <code>acquire</code> (L552). This is the heart of the pool.</p> <pre><code>flowchart TD\n    START([acquire called]) --&gt; READY[await _ready]\n    READY --&gt; OPTS[AcquireOptions.from_kwargs]\n    OPTS --&gt; LOOP{while True}\n\n    LOOP --&gt; LOCK[async with _state_lock]\n    LOCK --&gt; AVAIL[_check_availability]\n    AVAIL --&gt;|PoolCircuitOpenError| WAIT_CB[store exception, skip select]\n    AVAIL --&gt;|ok| SELECT[_select]\n    SELECT --&gt;|proxy found| MARK[_mark_acquired + hooks]\n    MARK --&gt; BREAK[break loop]\n    SELECT --&gt;|None| TIMEOUT{timeout budget?}\n\n    TIMEOUT --&gt;|timeout negative| WAIT_INF[_wait_for_availability None]\n    TIMEOUT --&gt;|remaining positive| WAIT[_wait_for_availability remaining]\n    WAIT_INF --&gt; LOOP\n    WAIT --&gt; LOOP\n\n    TIMEOUT --&gt;|exhausted| CLASSIFY{circuit open?}\n    CLASSIFY --&gt;|yes| RAISE_CB[raise PoolCircuitOpenError]\n    CLASSIFY --&gt;|no| META{missing metadata?}\n    META --&gt;|yes| RAISE_META[raise MissingProxyMetadata]\n    META --&gt;|no| REFRESH{on-demand refresh?}\n    REFRESH --&gt;|once| ODR[_attempt_on_demand_refresh]\n    ODR --&gt;|added proxies| LOOP\n    REFRESH --&gt;|no| FAIL[_classify_acquire_failure then raise]\n\n    BREAK --&gt; ROTATE{rotate_on_acquire?}\n    ROTATE --&gt;|yes| AROT[proxy.arotate]\n    ROTATE --&gt;|no| HOOKS[run_deferred hooks]\n    AROT --&gt; HOOKS\n    HOOKS --&gt; RETURN([return Proxy])\n</code></pre>"},{"location":"for_mine_and_only_my/#step-by-step-one-iteration","title":"Step-by-step (one iteration)","text":"<ol> <li><code>await self._ready.wait()</code> \u2014 pool must be started.</li> <li><code>AcquireOptions.from_kwargs</code> \u2014 merge caller filters with pool defaults (<code>tags</code>, <code>accept_callback</code>, <code>session_id</code> alias).</li> <li>Under <code>_state_lock</code>:</li> <li><code>_check_availability</code> \u2014 closed? draining? circuit open?</li> <li><code>_append_circuit_hooks</code> \u2014 queue open/close hook callbacks.</li> <li><code>_select(options)</code> \u2192 if proxy:<ul> <li><code>_mark_acquired</code> \u2014 bump lease, stats, session bind, HALF_OPEN probe tag.</li> <li>Queue <code>on_proxy_acquired</code>; maybe <code>on_session_rebind</code>.</li> <li>break out of loop.</li> </ul> </li> <li>If no proxy: wait on <code>_available_cond</code> (respects <code>acquire_timeout</code> and next cooldown wakeup via <code>_bounded_wait_timeout</code>).</li> <li>When timeout exhausted (still under lock logic, then outside):</li> <li>Circuit open \u2192 <code>PoolCircuitOpenError</code></li> <li>Missing required metadata \u2192 <code>MissingProxyMetadata</code></li> <li>Else one on-demand refresh via <code>_attempt_on_demand_refresh</code></li> <li>Else <code>_classify_acquire_failure</code> \u2192 <code>PoolExhausted</code> / <code>PoolSaturated</code> / <code>NoMatchingProxy</code></li> <li>After loop: optional <code>proxy.arotate()</code> if <code>rotate_on_acquire</code>; run deferred hooks; return proxy.</li> </ol>"},{"location":"for_mine_and_only_my/#waiting-helpers","title":"Waiting helpers","text":"Method Role <code>_bounded_wait_timeout</code> Min of fallback interval, user budget, next cooldown expiry <code>_wait_for_availability</code> <code>Condition.wait</code> with bounded timeout"},{"location":"for_mine_and_only_my/#6-after-acquire","title":"6. After acquire","text":"<pre><code>flowchart LR\n    ACQ[acquire returns Proxy] --&gt; USE[your HTTP request]\n\n    USE --&gt;|OK| PATH{how to return?}\n    PATH --&gt;|latency known| MS[mark_success]\n    PATH --&gt;|simple return| REL[release]\n\n    USE --&gt;|error| MF[mark_failed]\n\n    MS --&gt; DEC1[_return_lease count_release_stat=False]\n    REL --&gt; DEC2[_return_lease count_release_stat=True]\n    MF --&gt; DEC3[_return_lease count_release_stat=False]\n\n    MS --&gt; SC[update EMA, clear cooldown]\n    MS --&gt; CB1[circuit.record_success]\n\n    MF --&gt; FAIL[consecutive_failures++, EMA fail]\n    MF --&gt; CD[_apply_cooldown]\n    MF --&gt; CB2[circuit.record_failure]\n    MF --&gt; ROT[optional arotate]\n</code></pre> Method Lines Lease Stats Scoring Cooldown Circuit Hooks <code>release</code> L673 \u2713 released++ \u2014 \u2014 \u2014 clears HALF_OPEN probe <code>on_proxy_released</code> <code>mark_success</code> L750 \u2713 \u2014 EMA + clears <code>record_success</code> \u2014 <code>mark_failed</code> L698 \u2713 failed++ EMA \u2212 <code>_apply_cooldown</code> <code>record_failure</code> <code>on_proxy_failed</code> <p>Important: Health-check results (<code>_apply_check_result</code>) update scoring/cooldown but do not feed the circuit breaker \u2014 only client-reported <code>mark_success</code> / <code>mark_failed</code> do.</p>"},{"location":"for_mine_and_only_my/#7-selection-filtering-pipeline","title":"7. Selection &amp; filtering pipeline","text":"<p>Called from <code>_select</code> \u2192 <code>_get_eligible</code> \u2192 strategy.</p> <pre><code>flowchart TD\n    SEL[_select] --&gt; ELIG[_get_eligible]\n\n    ELIG --&gt; SK{session_key?}\n    SK --&gt;|yes| RES[resolve_session in session.py]\n    RES --&gt;|bound + ok| ONE[return single proxy]\n    RES --&gt;|bound + cooldown| POL{session cooldown_policy}\n    POL --&gt;|BLOCK| EMPTY[return empty]\n    POL --&gt;|RAISE| ERR[SessionBrokenError]\n    POL --&gt;|REBIND| PEND[stash pending_rebind, clear registry]\n\n    SK --&gt;|no / rebind path| SCAN[for each proxy in deque]\n    SCAN --&gt; CD{in cooldown?}\n    CD --&gt;|yes| SKIP[skip]\n    CD --&gt;|no| CAP{at connection cap?}\n    CAP --&gt;|yes| SKIP\n    CAP --&gt;|no| FIL[_sticky_filters_ok]\n    FIL --&gt;|pass| ADD[add to result]\n\n    ELIG --&gt; STRAT[_strategy.select eligible, scores, deque]\n</code></pre>"},{"location":"for_mine_and_only_my/#filter-helpers-all-synchronous-called-under-lock","title":"Filter helpers (all synchronous, called under lock)","text":"Method Lines Role <code>_active_metadata_filters</code> L906 country, min_anonymity, tags <code>_proxy_matches_metadata_filter</code> L928 Per-attribute match logic <code>_metadata_value_missing</code> L887 None / empty detection <code>_anonymity_rank</code> L870 transparent &lt; anonymous &lt; elite <code>_sticky_filters_ok</code> L1054 All metadata + <code>accept_callback</code> <code>_at_connection_cap</code> L1038 vs <code>limits.max_connections_per_proxy</code> <code>_any_filter_match</code> L1024 Any proxy passes filters (for error typing) <code>_missing_metadata_message</code> L960 For <code>FilterMissingMetadata.RAISE</code> <code>_classify_acquire_failure</code> L990 Pick Exhausted / Saturated / NoMatchingProxy"},{"location":"for_mine_and_only_my/#strategy-selection","title":"Strategy selection","text":"<p><code>_build_strategy</code> (L425) maps <code>PoolStrategy</code> enum \u2192 class in <code>strategies.py</code>:</p> <ul> <li><code>ROUND_ROBIN</code> \u2192 <code>RoundRobinStrategy</code> (uses full deque for fair rotation)</li> <li><code>RANDOM</code> \u2192 <code>RandomStrategy</code></li> <li><code>WEIGHTED</code> \u2192 <code>WeightedStrategy</code> (uses <code>_scores</code>)</li> <li><code>LOWEST_LATENCY</code> \u2192 <code>LowestLatencyStrategy</code> (uses <code>_scores</code>)</li> </ul>"},{"location":"for_mine_and_only_my/#8-background-workers","title":"8. Background workers","text":"<p>Started in <code>_start</code>, cancelled in <code>_stop_background_tasks</code>.</p> <pre><code>flowchart TB\n    subgraph health[\"_health_check_loop L1487\"]\n        H1[sleep check_interval]\n        H2[gather candidates not in cooldown]\n        H3[arun_health_check bounded by _health_sem]\n        H4[_apply_check_result under lock]\n        H1 --&gt; H2 --&gt; H3 --&gt; H4 --&gt; H1\n    end\n\n    subgraph refresh[\"_refresh_loop L1556\"]\n        R1{urgent _refresh_needed?}\n        R1 --&gt;|no| R2[sleep interval_seconds]\n        R1 --&gt;|yes| R3[_refresh_and_merge]\n        R2 --&gt; R3 --&gt; R1\n    end\n\n    subgraph dead_letter[\"_dead_letter_retrier L1536\"]\n        D1[retry_cycle in dead_letter.py]\n    end\n\n    subgraph metrics[\"_metrics_worker L1588\"]\n        M1[drain _metrics_queue]\n        M2[metrics_exporter.emit_gauge]\n        M1 --&gt; M2 --&gt; M1\n    end\n</code></pre> Worker Config gate Locking Health <code>config.health_check</code> Snapshot candidates outside lock; apply inside Refresh fetchers or refresh callbacks <code>_refresh_lock</code> \u2192 <code>_state_lock</code> on merge Dead letter <code>dead_letter.enabled</code> <code>retry_cycle</code> uses <code>_state_lock</code> Metrics <code>metrics_exporter</code> Queue only; worker isolated"},{"location":"for_mine_and_only_my/#9-refresh-merge","title":"9. Refresh &amp; merge","text":"<p>On-demand (from <code>acquire</code>) and periodic (background) share the same merge path.</p> <pre><code>_attempt_on_demand_refresh\n  \u2192 _refresh_lock (coalesce via _refresh_generation)\n  \u2192 _refresh_and_merge\n       \u2192 _fetch_new_proxies (refresh.py)\n       \u2192 _merge_new_proxies\n       \u2192 _check_min_size (may set _refresh_needed)\n</code></pre> Method Lines Role <code>_fetch_new_proxies</code> L1348 Callbacks beat fetchers <code>_refresh_and_merge</code> L1374 Hooks, fetch, merge, bump generation <code>_merge_new_proxies</code> L1416 Dedupe by URL; evict front if <code>max_size</code> <code>_evict_proxy</code> L1292 Clean all bookkeeping for one URL <code>_has_refresh_source</code> L1446 Fetchers or callbacks configured? <code>_check_min_size</code> L1465 Warn + flag urgent refresh"},{"location":"for_mine_and_only_my/#10-health-checks-dead-letter","title":"10. Health checks &amp; dead letter","text":""},{"location":"for_mine_and_only_my/#health-check-result-path","title":"Health check result path","text":"<p><code>_record_health_check_result</code> (async wrapper) \u2192 <code>_apply_check_result</code> (L1220):</p> <ul> <li>Proxy still in pool? (URL set membership)</li> <li>Success: clear failures, update EMA, clear cooldown, hooks <code>on_check_complete</code> + <code>on_proxy_recovered</code></li> <li>Failure: failed stat, failures++, EMA, <code>_apply_cooldown</code>, hooks <code>on_check_complete</code> + <code>on_proxy_failed</code></li> </ul>"},{"location":"for_mine_and_only_my/#cooldown-application","title":"Cooldown application","text":"<p><code>_apply_cooldown</code> (L1171):</p> <ol> <li>Skip if <code>consecutive_failures &lt; failure_threshold</code></li> <li>Duration from custom <code>cooldown.strategy</code> or <code>compute_cooldown</code> (<code>cooldown.py</code>)</li> <li>Store <code>monotonic() + duration</code> in <code>_cooldown_until</code></li> </ol>"},{"location":"for_mine_and_only_my/#warmup-startup-only","title":"Warmup (startup only)","text":"<p>Uses <code>_unchecked_proxies</code> + <code>run_warmup</code> (<code>warmup.py</code>) \u2014 not repeated here; runs before <code>_ready</code>.</p>"},{"location":"for_mine_and_only_my/#11-method-index-by-role","title":"11. Method index by role","text":"<p>Quick lookup \u2014 every method on <code>AsyncProxyPool</code>.</p>"},{"location":"for_mine_and_only_my/#lifecycle-context","title":"Lifecycle &amp; context","text":"Method Lines <code>__init__</code> 157 <code>__aenter__</code> 223 <code>__aexit__</code> 240 <code>_start</code> 256 <code>_close</code> 361 <code>close</code> 410 <code>_stop_background_tasks</code> 335"},{"location":"for_mine_and_only_my/#public-acquire-release-api","title":"Public acquire / release API","text":"Method Lines <code>acquire</code> 552 <code>release</code> 673 <code>mark_failed</code> 698 <code>mark_success</code> 750 <code>statistics</code> (property) 1650"},{"location":"for_mine_and_only_my/#availability-waiting","title":"Availability &amp; waiting","text":"Method Lines <code>_check_availability</code> 824 <code>_bounded_wait_timeout</code> 476 <code>_wait_for_availability</code> 507 <code>_return_lease</code> 526"},{"location":"for_mine_and_only_my/#selection-filters","title":"Selection &amp; filters","text":"Method Lines <code>_select</code> 851 <code>_get_eligible</code> 1079 <code>_mark_acquired</code> 1138 <code>_build_strategy</code> 425 <code>_sticky_filters_ok</code> 1054 <code>_active_metadata_filters</code> 906 <code>_proxy_matches_metadata_filter</code> 928 <code>_metadata_value_missing</code> 887 <code>_anonymity_rank</code> 870 <code>_at_connection_cap</code> 1038 <code>_any_filter_match</code> 1024 <code>_missing_metadata_message</code> 960 <code>_classify_acquire_failure</code> 990"},{"location":"for_mine_and_only_my/#failure-accounting","title":"Failure accounting","text":"Method Lines <code>_apply_cooldown</code> 1171 <code>_apply_check_result</code> 1220 <code>_record_health_check_result</code> 808 <code>_count_consecutive_failures</code> 1278 <code>_evict_proxy</code> 1292 <code>_unchecked_proxies</code> 794"},{"location":"for_mine_and_only_my/#refresh","title":"Refresh","text":"Method Lines <code>_attempt_on_demand_refresh</code> 1328 <code>_fetch_new_proxies</code> 1348 <code>_refresh_and_merge</code> 1374 <code>_merge_new_proxies</code> 1416 <code>_has_refresh_source</code> 1446 <code>_check_min_size</code> 1465 <code>_refresh_loop</code> 1556"},{"location":"for_mine_and_only_my/#background-metrics","title":"Background &amp; metrics","text":"Method Lines <code>_health_check_loop</code> 1487 <code>_dead_letter_retrier</code> 1536 <code>_metrics_worker</code> 1588 <code>_emit_stat_metric</code> 1607 <code>_enqueue_metric</code> 1629"},{"location":"for_mine_and_only_my/#hooks-circuit","title":"Hooks &amp; circuit","text":"Method Lines <code>_append_circuit_hooks</code> 451"},{"location":"for_mine_and_only_my/#supporting-types-same-file","title":"Supporting types (same file)","text":"Type Lines <code>AcquireOptions</code> 42 <code>AcquireOptions.from_kwargs</code> 72 <code>PoolStatistics</code> 110 <code>SyncProxyPool</code> 1663"},{"location":"for_mine_and_only_my/#12-external-modules","title":"12. External modules","text":"Module Used for <code>config.py</code> <code>PoolConfig</code> and all sub-configs <code>strategies.py</code> <code>SelectionStrategy</code> implementations <code>scoring.py</code> <code>EMAState</code>, <code>update_ema</code> <code>cooldown.py</code> <code>compute_cooldown</code>, <code>is_in_cooldown</code> <code>circuit_breaker.py</code> <code>CircuitBreaker</code>, <code>allow_request</code>, HALF_OPEN <code>session.py</code> <code>resolve_session</code>, <code>SessionEntry</code> <code>hooks.py</code> <code>run_deferred</code> for lifecycle callbacks <code>refresh.py</code> <code>fetch_from_fetchers</code>, <code>fetch_from_refresh_config</code> <code>warmup.py</code> Startup warmup <code>dead_letter.py</code> <code>retry_cycle</code> <code>extended_proxy.py</code> <code>Proxy</code>, <code>arun_health_check</code> <code>errors.py</code> Pool exception types <p>PlantUML diagrams (render with any PlantUML viewer): <code>docs/diagrams/omniproxy.puml</code> \u2014 see <code>omniproxy-acquire-sequence</code> and <code>omniproxy-classes</code> diagrams.</p>"},{"location":"for_mine_and_only_my/#13-exceptions","title":"13. Exceptions","text":""},{"location":"for_mine_and_only_my/#from-acquire","title":"From <code>acquire</code>","text":"Exception When <code>PoolClosedError</code> After <code>_closed</code> <code>PoolDrainingError</code> During shutdown drain <code>PoolCircuitOpenError</code> Breaker open; timeout exhausted while still open <code>PoolExhausted</code> No usable proxies (empty pool, all cooling down, etc.) <code>PoolSaturated</code> Matches exist but all at connection cap or cooling down <code>NoMatchingProxy</code> Filters exclude every proxy <code>MissingProxyMetadata</code> <code>filter_missing_metadata=RAISE</code> and required field missing on all <code>SessionBrokenError</code> Sticky session proxy in cooldown with <code>RAISE</code> policy <code>WarmupFailedError</code> Startup only, if warmup policy is <code>RAISE</code>"},{"location":"for_mine_and_only_my/#decision-tree-for-acquire-failure-after-timeout","title":"Decision tree for acquire failure (after timeout)","text":"<pre><code>_proxies empty?           \u2192 PoolExhausted\nfilters active &amp; no match \u2192 NoMatchingProxy\nall matching capped/CD?   \u2192 PoolSaturated or PoolExhausted\nelse                      \u2192 PoolExhausted\n</code></pre>"},{"location":"for_mine_and_only_my/#suggested-reading-order","title":"Suggested reading order","text":"<p>If you are tracing code for the first time:</p> <ol> <li><code>__init__</code> \u2014 know the fields</li> <li><code>_start</code> / <code>_close</code> \u2014 lifecycle</li> <li><code>acquire</code> \u2014 main loop (use \u00a75 flowchart)</li> <li><code>_get_eligible</code> + <code>_select</code> \u2014 who gets picked</li> <li><code>mark_success</code> / <code>mark_failed</code> / <code>release</code> \u2014 how state moves back</li> <li><code>_health_check_loop</code> + <code>_refresh_loop</code> \u2014 background maintenance</li> <li>Jump to <code>circuit_breaker.py</code>, <code>session.py</code>, <code>strategies.py</code> as needed</li> </ol>"},{"location":"adr/","title":"Architecture Decision Records","text":"<p>This directory captures the significant architectural decisions behind omniproxy (v4).</p> <p>Each ADR follows a short MADR-style template:</p> Field Meaning Status Proposed \u00b7 Accepted \u00b7 Deprecated \u00b7 Superseded Context Why a decision was needed Decision What we chose Consequences What follows from that choice"},{"location":"adr/#index","title":"Index","text":"ADR Title Status 0001 Library scope: client-side proxy toolkit, not a proxy server Accepted 0002 <code>Proxy</code> as an immutable <code>str</code> subclass Accepted 0003 Pluggable HTTP backends via optional extras Accepted 0004 Async-first pool; sync via event-loop bridge Accepted 0005 Explicit acquire / release | mark_success | mark_failed lifecycle Accepted 0006 Frozen Pydantic config graphs with opinionated presets Accepted 0007 Pool-wide circuit breaker fed only by client outcomes Accepted 0008 Pluggable selection strategies and optional EMA scoring Accepted 0009 Sticky sessions and rotation URLs for residential/mobile Accepted 0010 Pluggable fetchers, refresh, warmup, and dead-letter Accepted"},{"location":"adr/#related-docs","title":"Related docs","text":"<ul> <li>Package overview: <code>README.md</code></li> <li>Pool flows: <code>../pool-flows/</code></li> <li>Architecture diagram: <code>../diagrams/omniproxy.puml</code></li> </ul>"},{"location":"adr/0001-library-scope-not-proxy-server/","title":"ADR-0001: Library scope \u2014 client-side proxy toolkit, not a proxy server","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: scope, product</li> </ul>"},{"location":"adr/0001-library-scope-not-proxy-server/#context","title":"Context","text":"<p>Proxy tooling often collapses into one of two products:</p> <ol> <li>A network gateway (accept inbound traffic, terminate TLS, forward upstream).</li> <li>A client library (parse proxy strings, check reachability, orchestrate outbound use).</li> </ol> <p>Callers of omniproxy are scrapers, automation, and API clients that already own their HTTP stack. They need a canonical proxy type, health checks, and pool orchestration\u2014not another hop in the network path.</p>"},{"location":"adr/0001-library-scope-not-proxy-server/#decision","title":"Decision","text":"<p>Ship omniproxy as an embeddable Python library (plus a small CLI for ops), not as a reverse/forward proxy process.</p> <p>Responsibilities in scope:</p> <ul> <li>Parse many proxy string formats into a structured <code>Proxy</code>.</li> <li>Check proxies through pluggable HTTP clients.</li> <li>Lease and manage pools (<code>AsyncProxyPool</code> / <code>SyncProxyPool</code>).</li> <li>Fetch/scrape proxy lists; expose <code>omniproxy check</code> / <code>scrape</code>.</li> </ul> <p>Out of scope:</p> <ul> <li>Listening sockets, TLS termination, request rewriting.</li> <li>Built-in Redis/Postgres persistence or multi-process control planes.</li> <li>Application auth (OAuth/JWT); credentials live in proxy URLs only.</li> </ul>"},{"location":"adr/0001-library-scope-not-proxy-server/#consequences","title":"Consequences","text":"<ul> <li>Positive: Thin dependency surface; fits any HTTP stack; clear mental model for library users.</li> <li>Negative: Operators who want a standalone proxy gateway must use other tools.</li> <li>Neutral: Persistence and metrics are extension points (<code>StateStore</code>, <code>MetricsExporter</code>), not core services.</li> </ul>"},{"location":"adr/0001-library-scope-not-proxy-server/#evidence","title":"Evidence","text":"<ul> <li><code>README.md</code>, <code>omniproxy/__init__.py</code></li> <li><code>pyproject.toml</code> (<code>libraries</code> classifiers; CLI entry points)</li> </ul>"},{"location":"adr/0002-proxy-as-str-subclass/","title":"ADR-0002: <code>Proxy</code> as an immutable <code>str</code> subclass","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: core-model, parsing</li> </ul>"},{"location":"adr/0002-proxy-as-str-subclass/#context","title":"Context","text":"<p>Downstream code stores proxies in configs, logs, env vars, and HTTP client <code>proxies=</code> maps. A rich object (dataclass / Pydantic model) forces adapters everywhere. A bare string loses structure (protocol, auth, rotation URL, metadata).</p> <p>Parsing must also accept many wire formats (<code>host:port</code>, auth variants, full URLs, SOCKS, bracketed IPv6, trailing <code>[rotation_url]</code>).</p>"},{"location":"adr/0002-proxy-as-str-subclass/#decision","title":"Decision","text":"<p>Represent a proxy as an immutable <code>str</code> subclass (<code>Proxy</code>) whose string value is the canonical URL, with structural and metadata fields on <code>__slots__</code>.</p> <ul> <li>Construction goes through <code>OmniproxyParser</code> (msgspec <code>Struct</code> + compiled format regexes).</li> <li>Structural fields are read-only after construction.</li> <li>Metadata (latency, anonymity, geo) is written only via internal <code>_set_attribute</code> / check helpers.</li> <li>Helpers (<code>url</code>, <code>safe_url</code>, <code>as_requests_proxies</code>, Playwright TypedDict) adapt to common clients without changing the core type.</li> </ul>"},{"location":"adr/0002-proxy-as-str-subclass/#consequences","title":"Consequences","text":"<ul> <li>Positive: Drop-in for string APIs; structured access when needed; one canonical form after parse.</li> <li>Negative: Subclassing <code>str</code> has quirks (<code>isinstance</code>, hashing, pickle); mutation rules must stay disciplined.</li> <li>Neutral: Format regexes are a maintenance surface as new vendor formats appear.</li> </ul>"},{"location":"adr/0002-proxy-as-str-subclass/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Plain dataclass / Pydantic model Requires adapters for every string-shaped API TypedDict only Weak runtime validation; poor method surface Separate \u201craw string\u201d + \u201cinfo\u201d types Doubles APIs and invites drift"},{"location":"adr/0002-proxy-as-str-subclass/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/proxy.py</code>, <code>omniproxy/extended_proxy.py</code></li> <li><code>omniproxy/utils.py</code> (<code>OmniproxyParser</code>)</li> <li><code>omniproxy/constants.py</code> (<code>PROXY_FORMATS_REGEXP</code>)</li> </ul>"},{"location":"adr/0003-pluggable-http-backends/","title":"ADR-0003: Pluggable HTTP backends via optional extras","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: networking, packaging</li> </ul>"},{"location":"adr/0003-pluggable-http-backends/#context","title":"Context","text":"<p>Proxy checks and anonymity probes need an HTTP client. Users already standardize on different stacks (httpx, aiohttp, requests, curl-cffi, tls-client) for TLS fingerprinting, SOCKS support, or sync vs async style.</p> <p>Bundling every client as a hard dependency bloats installs and creates version conflicts. Pinning a single client forces stack churn on adopters.</p>"},{"location":"adr/0003-pluggable-http-backends/#decision","title":"Decision","text":"<ol> <li>Keep the runtime wheel lean: <code>msgspec</code>, <code>orjson</code>, <code>typing-extensions</code>.</li> <li>Expose a <code>BaseBackend</code> adapter and <code>get_backend(name)</code> factory.</li> <li>Ship each HTTP client as an optional extra (<code>[httpx]</code>, <code>[aiohttp]</code>, <code>[requests]</code>, <code>[curl_cffi]</code>, <code>[tls_client]</code>, plus <code>[re2]</code> / <code>[scrape]</code> / <code>[all]</code>).</li> <li>Raise a clear <code>ImportError</code> with install guidance when the chosen backend is missing.</li> <li>Prefer <code>curl_cffi</code> as the library default (<code>DEFAULT_BACKEND</code>) for stronger default TLS fingerprint behavior; docs/examples may still showcase httpx as a common install path.</li> </ol>"},{"location":"adr/0003-pluggable-http-backends/#consequences","title":"Consequences","text":"<ul> <li>Positive: Users install only what they need; one check API across backends; SOCKS/TLS strategies stay swappable.</li> <li>Negative: \u201cWorks after <code>pip install omniproxy</code>\u201d does not imply checks work until an extra is installed.</li> <li>Neutral: Backend feature parity is best-effort; some clients are sync-only or lack certain transports.</li> </ul>"},{"location":"adr/0003-pluggable-http-backends/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Single pinned HTTP client Rejects large share of existing stacks Fat wheel with all clients Heavy, conflict-prone installs Require user-supplied transport callbacks only Too much glue for the common case"},{"location":"adr/0003-pluggable-http-backends/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/backends/base.py</code>, <code>omniproxy/backends/factory.py</code></li> <li><code>omniproxy/constants.py</code> (<code>DEFAULT_BACKEND</code>, <code>SUPPORTED_BACKENDS</code>)</li> <li><code>pyproject.toml</code> (<code>optional-dependencies</code>)</li> </ul>"},{"location":"adr/0004-async-first-pool-sync-bridge/","title":"ADR-0004: Async-first pool; sync via event-loop bridge","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: concurrency, pool</li> </ul>"},{"location":"adr/0004-async-first-pool-sync-bridge/#context","title":"Context","text":"<p>Pool orchestration needs concurrent background work: health checks, refresh, dead-letter retry, metrics. Maintaining separate sync and async pool implementations doubles bugs and drifts behavior.</p> <p>Many callers still use synchronous code (scripts, requests-based pipelines).</p>"},{"location":"adr/0004-async-first-pool-sync-bridge/#decision","title":"Decision","text":"<ul> <li>Implement <code>AsyncProxyPool</code> as the sole pool engine (leases, filters, strategies, circuit breaker, background tasks).</li> <li>Provide <code>SyncProxyPool</code> as a thin wrapper: a daemon thread owning a private asyncio event loop, delegating every public call into <code>AsyncProxyPool</code>.</li> <li>Keep the historical alias <code>ProxyPool = SyncProxyPool</code> for older callers.</li> </ul>"},{"location":"adr/0004-async-first-pool-sync-bridge/#consequences","title":"Consequences","text":"<ul> <li>Positive: One source of truth for pool semantics; async apps get native performance; sync apps keep a familiar blocking API.</li> <li>Negative: Sync path pays thread + loop overhead and cross-thread scheduling complexity.</li> <li>Neutral: Sync and async APIs must stay intentionally mirrored; new features land on async first.</li> </ul>"},{"location":"adr/0004-async-first-pool-sync-bridge/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Dual sync/async implementations Drift and double maintenance Async-only public API Breaks large sync audience Multiprocess pool Overkill for in-process lease management"},{"location":"adr/0004-async-first-pool-sync-bridge/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/pool.py</code> (<code>AsyncProxyPool</code>, <code>SyncProxyPool</code>)</li> <li><code>omniproxy/__init__.py</code> (<code>ProxyPool</code> alias)</li> <li><code>docs/for_mine_and_only_my.md</code>, <code>docs/diagrams/omniproxy.puml</code></li> </ul>"},{"location":"adr/0005-explicit-lease-lifecycle/","title":"ADR-0005: Explicit acquire / release | mark_success | mark_failed lifecycle","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: pool, api</li> </ul>"},{"location":"adr/0005-explicit-lease-lifecycle/#context","title":"Context","text":"<p>A pool that \u201chands out\u201d proxies must track active leases, connection caps, scoring, cooldowns, and circuit-breaker signals. Silent auto-release on GC is unreliable. Implicit success-on-any-return conflates \u201cdone using\u201d with \u201crequest succeeded.\u201d</p> <p>Callers also need different outcomes:</p> <ul> <li>finished without a quality signal (<code>release</code>)</li> <li>successful use (<code>mark_success</code>)</li> <li>failed use with exception context (<code>mark_failed</code>)</li> </ul>"},{"location":"adr/0005-explicit-lease-lifecycle/#decision","title":"Decision","text":"<p>Treat pool membership as an explicit lease:</p> <ol> <li><code>acquire(...)</code> returns a <code>Proxy</code> and increments the lease counter.</li> <li>Exactly one of <code>release</code>, <code>mark_success</code>, or <code>mark_failed</code> must follow per acquisition.</li> <li><code>mark_*</code> updates scores / cooldowns / circuit breaker and returns the lease (do not also call <code>release</code>).</li> <li>Optional convenience flags (<code>auto_mark_success_on_exit</code>, <code>auto_mark_failed_on_exception</code>) may automate this inside context helpers without changing the underlying model.</li> <li>Filters and sticky sessions are expressed via <code>AcquireOptions</code> at acquire time.</li> </ol>"},{"location":"adr/0005-explicit-lease-lifecycle/#consequences","title":"Consequences","text":"<ul> <li>Positive: Deterministic accounting; clear failure semantics; breaker and scoring stay accurate.</li> <li>Negative: Misuse (double release, or release + mark_success) is a footgun; docs must stress the rule.</li> <li>Neutral: Context-manager helpers can reduce boilerplate but must map cleanly onto the same lifecycle.</li> </ul>"},{"location":"adr/0005-explicit-lease-lifecycle/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Auto-release via finalizers Non-deterministic; hides failures Single <code>done(success: bool)</code> API Loses distinction between \u201creleased unused\u201d and \u201csucceeded\u201d Checkout without feedback Cannot drive cooldown / scoring / breaker"},{"location":"adr/0005-explicit-lease-lifecycle/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/pool.py</code> (<code>acquire</code>, <code>release</code>, <code>mark_success</code>, <code>mark_failed</code>, <code>AcquireOptions</code>)</li> <li><code>docs/for_mine_and_only_my.md</code> (usage lifecycle)</li> </ul>"},{"location":"adr/0006-frozen-pydantic-config-presets/","title":"ADR-0006: Frozen Pydantic config graphs with opinionated presets","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: configuration</li> </ul>"},{"location":"adr/0006-frozen-pydantic-config-presets/#context","title":"Context","text":"<p>Pool behavior has many interacting knobs (cooldown, limits, sessions, scoring, breaker, refresh, warmup). Mutable nested dicts invite partial updates and racey hot-mutation. Users also need starting points for common workloads (scraping, API gateways, stealth, residential rotation, load balancing).</p>"},{"location":"adr/0006-frozen-pydantic-config-presets/#decision","title":"Decision","text":"<ol> <li>Model process defaults as a frozen <code>GlobalConfig</code> singleton (<code>settings</code>).</li> <li>Model pool policy as a frozen <code>PoolConfig</code> graph of nested config models (<code>CooldownConfig</code>, <code>SessionConfig</code>, \u2026).</li> <li>Apply changes by replacement (<code>model_copy(update=...)</code>), not in-place mutation.</li> <li>Ship opinionated presets as classmethods:</li> <li><code>scraping_preset</code></li> <li><code>api_gateway_preset</code></li> <li><code>stealth_preset</code></li> <li><code>rotating_residential_preset</code></li> <li><code>load_balancer_preset</code></li> <li>Validate cross-field consistency in model validators (e.g. scoring required for weighted / lowest-latency strategies).</li> </ol>"},{"location":"adr/0006-frozen-pydantic-config-presets/#consequences","title":"Consequences","text":"<ul> <li>Positive: Safe sharing across tasks; clearer diffs when swapping policy; presets encode known-good combinations.</li> <li>Negative: Nested models are verbose; callers must learn <code>model_copy</code> for tweaks.</li> <li>Neutral: Presets are starting points, not hard product modes\u2014any field can still be overridden via copy.</li> </ul>"},{"location":"adr/0006-frozen-pydantic-config-presets/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Mutable singleton config Racey; hard to reason about mid-flight changes YAML/TOML-only config files Extra I/O layer; Python call sites still need objects Flat kwargs on the pool constructor Does not scale to nested policies"},{"location":"adr/0006-frozen-pydantic-config-presets/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/config.py</code> (<code>GlobalConfig</code>, <code>PoolConfig</code>, presets)</li> </ul>"},{"location":"adr/0007-circuit-breaker-client-outcomes-only/","title":"ADR-0007: Pool-wide circuit breaker fed only by client outcomes","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: resilience, pool</li> </ul>"},{"location":"adr/0007-circuit-breaker-client-outcomes-only/#context","title":"Context","text":"<p>When many proxies fail, continuing to acquire burns time and amplifies outages. A circuit breaker can shed load and allow recovery probes.</p> <p>Health checks also produce failures, but they are synthetic: probing a check URL is not the same as a caller\u2019s real request failing. Feeding the breaker from health checks can open the circuit during quiet periods or because a probe endpoint is flaky, even when production traffic is fine.</p>"},{"location":"adr/0007-circuit-breaker-client-outcomes-only/#decision","title":"Decision","text":"<ul> <li>Offer an optional pool-wide <code>CircuitBreaker</code> (CLOSED / OPEN / HALF_OPEN).</li> <li>Feed it only from client-reported outcomes via <code>mark_success</code> / <code>mark_failed</code>.</li> <li>Do not feed the breaker from the health-check loop; health checks update failure counts, scoring EMA, and cooldowns only.</li> <li>In HALF_OPEN, allow a single probe acquisition and gate others with <code>PoolCircuitOpenError</code>.</li> </ul>"},{"location":"adr/0007-circuit-breaker-client-outcomes-only/#consequences","title":"Consequences","text":"<ul> <li>Positive: Breaker reflects real workload health; health probes cannot false-trip shedding.</li> <li>Negative: If callers forget to <code>mark_failed</code>, the breaker stays silent.</li> <li>Neutral: Per-proxy isolation remains the job of cooldowns / scoring eviction, not the pool-wide breaker.</li> </ul>"},{"location":"adr/0007-circuit-breaker-client-outcomes-only/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Health checks trip the breaker Couples probe endpoint health to traffic shedding Per-proxy circuit breakers only Misses pool-wide cascading failure signal No breaker Acquire storms during outages"},{"location":"adr/0007-circuit-breaker-client-outcomes-only/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/circuit_breaker.py</code></li> <li><code>omniproxy/pool.py</code> (<code>_apply_health_result</code> notes; <code>mark_success</code> / <code>mark_failed</code>)</li> <li><code>docs/diagrams/omniproxy.puml</code></li> </ul>"},{"location":"adr/0008-selection-strategies-and-ema-scoring/","title":"ADR-0008: Pluggable selection strategies and optional EMA scoring","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: pool, selection</li> </ul>"},{"location":"adr/0008-selection-strategies-and-ema-scoring/#context","title":"Context","text":"<p>Different workloads need different pick policies:</p> <ul> <li>even spread (round-robin)</li> <li>cheap randomness</li> <li>prefer historically good proxies (weighted)</li> <li>prefer low latency</li> </ul> <p>Hard-coding one policy would force forks. Weighted / latency policies need a shared success/latency signal without requiring every strategy to invent its own stats store.</p>"},{"location":"adr/0008-selection-strategies-and-ema-scoring/#decision","title":"Decision","text":"<ol> <li>Define selection behind a <code>SelectionStrategy</code> protocol, built from <code>PoolConfig.strategy</code>.</li> <li>Ship built-ins: round-robin, random, weighted, lowest-latency.</li> <li>Maintain optional EMA scoring state (<code>EMAState</code>) updated on <code>mark_success</code> / <code>mark_failed</code> (and health outcomes for eligibility/eviction).</li> <li>Require scoring config when the chosen strategy needs it (validated on <code>PoolConfig</code>).</li> <li>Keep the in-memory container as a <code>deque[Proxy]</code> with eligibility filtering (cooldown, caps, tags, anonymity, sessions) before strategy selection.</li> </ol>"},{"location":"adr/0008-selection-strategies-and-ema-scoring/#consequences","title":"Consequences","text":"<ul> <li>Positive: Workload-appropriate picking without pool forks; scoring is reusable across strategies and eviction.</li> <li>Negative: Cold start needs <code>min_samples</code> / grace behavior; misconfigured weighted strategy without scoring is a config error.</li> <li>Neutral: Custom strategies can be added later without changing acquire\u2019s outer loop.</li> </ul>"},{"location":"adr/0008-selection-strategies-and-ema-scoring/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Single hard-coded strategy Insufficient for scraping vs LB vs latency-sensitive APIs External load balancer only Leaves the library without in-process orchestration Strategy-owned stats maps Duplicates state and complicates mark_* paths"},{"location":"adr/0008-selection-strategies-and-ema-scoring/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/strategies.py</code>, <code>omniproxy/scoring.py</code></li> <li><code>omniproxy/config.py</code> (<code>PoolStrategy</code>, <code>ScoringConfig</code>, validators)</li> <li><code>omniproxy/pool.py</code> (<code>_select</code>, eligibility pipeline)</li> </ul>"},{"location":"adr/0009-sticky-sessions-and-rotation-urls/","title":"ADR-0009: Sticky sessions and rotation URLs for residential/mobile","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: sessions, residential, mobile</li> </ul>"},{"location":"adr/0009-sticky-sessions-and-rotation-urls/#context","title":"Context","text":"<p>Some targets require the same egress IP across a logical session (login flows, carts, anti-bot continuity). Separately, residential/mobile providers expose a rotation URL that forces IP change without changing the proxy endpoint string.</p> <p>These concerns interact with cooldown: if a sticky proxy cools down mid-session, the pool must choose a policy (block, rebind, or raise) rather than silently breaking affinity.</p>"},{"location":"adr/0009-sticky-sessions-and-rotation-urls/#decision","title":"Decision","text":"<ol> <li>Support sticky sessions via <code>session_key</code> on acquire, backed by an in-memory session registry with TTL and rebind hooks.</li> <li>Define session cooldown policies: <code>BLOCK</code>, <code>REBIND</code>, <code>RAISE</code>.</li> <li>Parse optional trailing <code>[rotation_url]</code> into <code>Proxy.rotation_url</code> and expose <code>rotate</code> / <code>arotate</code>.</li> <li>Let pool config control when rotation fires (<code>use_rotation_urls</code>, <code>rotate_on_acquire</code>, <code>rotate_on_failure</code>), with a dedicated <code>rotating_residential_preset</code>.</li> <li>Keep the public rename path <code>session_id</code> \u2192 <code>session_key</code> for compatibility during the 4.x transition.</li> </ol>"},{"location":"adr/0009-sticky-sessions-and-rotation-urls/#consequences","title":"Consequences","text":"<ul> <li>Positive: First-class support for session affinity and provider rotation without app-specific glue.</li> <li>Negative: Sticky maps are process-local; multi-process affinity needs an external store.</li> <li>Neutral: Rotation is best-effort against provider APIs; failures surface through normal mark_failed / hooks.</li> </ul>"},{"location":"adr/0009-sticky-sessions-and-rotation-urls/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not App-managed sticky maps only Reimplements the same registry in every project Always rotate on every acquire Breaks session affinity and burns provider quotas Built-in distributed session store Out of library scope (see ADR-0001)"},{"location":"adr/0009-sticky-sessions-and-rotation-urls/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/session.py</code>, <code>omniproxy/proxy.py</code> (<code>rotation_url</code>, <code>rotate</code> / <code>arotate</code>)</li> <li><code>omniproxy/config.py</code> (<code>SessionConfig</code>, <code>rotating_residential_preset</code>)</li> <li><code>omniproxy/pool.py</code> (session resolve / rebind paths)</li> </ul>"},{"location":"adr/0010-pluggable-fetchers-refresh-dead-letter/","title":"ADR-0010: Pluggable fetchers, refresh, warmup, and dead-letter","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: replenishment, resilience</li> </ul>"},{"location":"adr/0010-pluggable-fetchers-refresh-dead-letter/#context","title":"Context","text":"<p>Pools shrink as proxies die, cool down, or get evicted. Callers source replacements from files, HTTP lists, or HTML pages. Coupling the pool to one source format makes reuse hard.</p> <p>Failed proxies also need a holding area so they are not immediately re-served, with optional retry and optional durable storage.</p>"},{"location":"adr/0010-pluggable-fetchers-refresh-dead-letter/#decision","title":"Decision","text":"<ol> <li>Define a <code>ProxyFetcher</code> protocol (<code>async def fetch() -&gt; list[Proxy | str]</code>).</li> <li>Ship built-ins: file, URL, and scrape fetchers.</li> <li>Drive replenishment through background refresh and on-demand refresh when below <code>min_size</code>, merging under locks with generation coalescing.</li> <li>Offer optional warmup before the pool becomes ready.</li> <li>Keep removed/failing proxies in a dead-letter queue with retry worker; persistence is optional via a <code>StateStore</code> protocol / factory\u2014not a built-in database.</li> </ol>"},{"location":"adr/0010-pluggable-fetchers-refresh-dead-letter/#consequences","title":"Consequences","text":"<ul> <li>Positive: Any source can plug in; pool size stays healthy without app timers; dead-letter avoids hot-looping bad endpoints.</li> <li>Negative: Fetcher quality and scrape fragility remain the caller\u2019s/source\u2019s problem.</li> <li>Neutral: In-memory dead-letter is the default; durable stores are opt-in integrations.</li> </ul>"},{"location":"adr/0010-pluggable-fetchers-refresh-dead-letter/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Hard-coded file-only refresh Ignores URL/list providers and scrapers Built-in Redis/SQLite dead-letter Violates lean library scope; wrong for many embeds No dead-letter (hard delete only) Loses recoverable proxies and observability"},{"location":"adr/0010-pluggable-fetchers-refresh-dead-letter/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/fetchers/</code> (<code>base.py</code>, file/url/scrape fetchers)</li> <li><code>omniproxy/refresh.py</code>, <code>omniproxy/warmup.py</code>, <code>omniproxy/dead_letter.py</code></li> <li><code>omniproxy/config.py</code> (<code>RefreshConfig</code>, <code>WarmupConfig</code>, <code>DeadLetterConfig</code>, <code>StateStore</code>)</li> </ul>"},{"location":"adr/0011-thread-safety-concurrency-guarantees/","title":"0011 thread safety concurrency guarantees","text":"<pre><code># ADR-0011: Thread\u2011safety and concurrency guarantees\n</code></pre> <ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: concurrency, pool, api-contract</li> </ul>"},{"location":"adr/0011-thread-safety-concurrency-guarantees/#context","title":"Context","text":"<p><code>AsyncProxyPool</code> manages mutable state (leases, scoring, cooldowns, session registry) and runs background coroutines. <code>SyncProxyPool</code> wraps this async engine inside a daemon thread holding a private event loop. Users inevitably share pool instances across threads, <code>concurrent.futures</code> executors, or async tasks. Without an explicit contract they will make unsafe assumptions (fork\u2011safety, mixing sync and async calls on the same pool, calling <code>acquire</code> from two threads simultaneously).</p>"},{"location":"adr/0011-thread-safety-concurrency-guarantees/#decision","title":"Decision","text":"<ol> <li> <p><code>AsyncProxyPool</code> is safe for concurrent use from multiple asyncio tasks executing in the same event loop. Internal mutations are protected by <code>asyncio.Lock</code> where needed. It is not designed for direct use from multiple event loops or threads without an external synchronisation wrapper.</p> </li> <li> <p><code>SyncProxyPool</code> provides a thread\u2011safe fa\u00e7ade: every public method dispatches to the private loop through a <code>threading.Lock</code>. This means multiple threads may call <code>acquire</code> / <code>release</code> / <code>mark_success</code> / <code>mark_failed</code> concurrently.</p> </li> <li> <p>Not fork\u2011safe: neither pool survives <code>os.fork()</code>. The daemon thread and event loop are not recreated after fork; users must instantiate fresh pools in child processes.</p> </li> <li> <p>Not re\u2011entrant into async code: do not call <code>SyncProxyPool</code> methods from within a coroutine running in the same event loop as the private one (it would deadlock). Users mixing styles should keep the async pool as the single source and use <code>asyncio.to_thread</code> if needed.</p> </li> <li> <p>User\u2011supplied hooks (e.g. <code>on_acquire</code>, <code>on_release</code>, fetcher callbacks) execute inside the pool\u2019s event loop. Callers must ensure they are safe in that context.</p> </li> </ol>"},{"location":"adr/0011-thread-safety-concurrency-guarantees/#consequences","title":"Consequences","text":"<ul> <li>Positive: Clear safety boundaries; thread\u2011safe sync API lets scripts and threading\u2011based apps share a pool; async users get efficient native orchestration.</li> <li>Negative: Sync overhead (lock + cross\u2011thread call) under heavy contention; fork\u2011safety limitation must be prominently documented.</li> <li>Neutral: The internal lock and loop ownership may be revisited if a future version adds a <code>ProcessPool</code> variant.</li> </ul>"},{"location":"adr/0011-thread-safety-concurrency-guarantees/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not No thread\u2011safety guarantees (caller must lock) Forces every user to reinvent synchronisation; pool state would be corrupted easily. Multiprocessing\u2011safe pool Out of library scope; adds heavy IPC dependencies. Allow <code>SyncProxyPool</code> inside async code Creates deadlock hazards and violates the single\u2011loop ownership model."},{"location":"adr/0011-thread-safety-concurrency-guarantees/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/pool.py</code> (<code>SyncProxyPool</code> lock implementation, daemon thread lifecycle)</li> <li><code>omniproxy/pool.py</code> (<code>AsyncProxyPool</code> internal <code>asyncio.Lock</code> for scoring/leasing)</li> <li><code>docs/for_mine_and_only_my.md</code> (user guide mentions threading constraints)</li> </ul>"},{"location":"adr/0012-extension-point-architecture/","title":"ADR-0012: Extension point architecture (<code>StateStore</code>, <code>MetricsExporter</code>, custom fetchers)","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: architecture, plugins, persistence</li> </ul>"},{"location":"adr/0012-extension-point-architecture/#context","title":"Context","text":"<p>ADR\u20110001 scopes omniproxy as an embeddable library without built\u2011in persistence or metric pipelines. Yet multiple extension hooks are already implied: <code>StateStore</code> for dead\u2011letter durability (ADR\u20110010), <code>MetricsExporter</code> for structured observability, and the <code>ProxyFetcher</code> protocol (ADR\u20110010). Without a unified extension policy, third\u2011party integrations may depend on private APIs or drift as the library evolves.</p>"},{"location":"adr/0012-extension-point-architecture/#decision","title":"Decision","text":"<ol> <li>Define a small, stable set of <code>typing.Protocol</code> interfaces that form the public extension surface:</li> <li><code>ProxyFetcher</code> (already present, <code>async def fetch() -&gt; list[Proxy | str]</code>)</li> <li><code>StateStore</code> (optional persistence for dead\u2011letter, sessions, or scoring)</li> <li> <p><code>MetricsExporter</code> (optional hook for structured metrics emission)</p> </li> <li> <p>Accept these interfaces in <code>PoolConfig</code> fields (e.g. <code>state_store_factory: Callable[[], StateStore]</code>), not via global registration. The pool calls them at well\u2011defined lifecycle events and never caches or mutates them outside the documented contract.</p> </li> <li> <p>No discovery mechanism (entry points, setuptools plugins) is shipped in the library itself. Third\u2011party packages can advertise their implementations through their own docs; the integrator explicitly passes instances to the pool.</p> </li> <li> <p>Internal implementation details (e.g. the concrete class behind <code>StateStore</code>) are not part of the public API and may change. The protocol alone defines backward compatibility.</p> </li> <li> <p>Custom selection strategies (ADR\u20110008) follow the same pattern: implement the <code>SelectionStrategy</code> protocol and pass it to <code>PoolConfig.strategy</code>.</p> </li> </ol>"},{"location":"adr/0012-extension-point-architecture/#consequences","title":"Consequences","text":"<ul> <li>Positive: Clear contract for external extensions; core team avoids bundling Redis/Postgres while still enabling them; integrators know exactly what they can swap.</li> <li>Negative: No automatic plugin discovery; users must wire components manually.</li> <li>Neutral: The set of protocols may grow slowly; each new protocol requires an ADR amendment.</li> </ul>"},{"location":"adr/0012-extension-point-architecture/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Abstract base classes Heavier; <code>Protocol</code> matches the \u201canything with the right methods\u201d philosophy. Global registry + entry points Adds unnecessary framework weight; violates \u201clibrary, not platform\u201d scope. No defined interfaces (duck typing only) Fails to provide discoverability; callers guess method signatures."},{"location":"adr/0012-extension-point-architecture/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/fetchers/base.py</code> (<code>ProxyFetcher</code>)</li> <li><code>omniproxy/dead_letter.py</code> (comments referencing <code>StateStore</code>)</li> <li><code>omniproxy/config.py</code> (<code>PoolConfig</code> fields for <code>state_store_factory</code> and <code>metrics_exporter</code>)</li> </ul>"},{"location":"adr/0013-error-taxonomy-exception-hierarchy/","title":"ADR-0013: Error taxonomy and exception hierarchy","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: errors, api-design</li> </ul>"},{"location":"adr/0013-error-taxonomy-exception-hierarchy/#context","title":"Context","text":"<p>The library performs I/O (checks, fetchers, rotation URLs), enforces internal constraints (circuit open, max leases, cooldowns), and validates configuration. Callers need to catch specific errors without importing every internal exception type. Currently exceptions exist ad\u2011hoc; without a documented hierarchy, additions could break existing <code>except</code> blocks.</p>"},{"location":"adr/0013-error-taxonomy-exception-hierarchy/#decision","title":"Decision","text":"<ol> <li> <p>Root exception: <code>OmniproxyError</code> (inherits from <code>Exception</code>). All library\u2011raised exceptions are descendants of this.</p> </li> <li> <p>First\u2011level families:</p> </li> <li><code>ProxyCheckError</code> \u2013 failure during a proxy reachability / anonymity check.</li> <li><code>PoolError</code> \u2013 any pool\u2011related runtime error (subtypes: <code>PoolCircuitOpenError</code>, <code>PoolExhaustedError</code>, <code>PoolLeaseError</code>).</li> <li><code>FetcherError</code> \u2013 failure inside a <code>ProxyFetcher</code> implementation (wraps underlying I/O or parse issues).</li> <li><code>BackendError</code> \u2013 when an HTTP backend raises an exception; always wraps the original client library exception.</li> <li> <p><code>ConfigError</code> \u2013 invalid pool or global configuration (raised at construction time).</p> </li> <li> <p>Wrapping rule: backend\u2011specific exceptions (e.g. <code>httpx.ConnectError</code>, <code>aiohttp.ClientConnectorError</code>) are never allowed to leak directly to the caller. They must be caught and re\u2011raised as <code>BackendError</code> with <code>cause</code> chaining.</p> </li> <li> <p>Safe stringification: exceptions that hold a <code>Proxy</code> must use <code>proxy.safe_url</code> in their <code>__str__</code> to prevent credential leakage into logs.</p> </li> <li> <p>Payload access: all exceptions carry a machine\u2011readable attribute where relevant (e.g. <code>proxy</code> string, <code>backend_name</code>, <code>retry_count</code>).</p> </li> </ol>"},{"location":"adr/0013-error-taxonomy-exception-hierarchy/#consequences","title":"Consequences","text":"<ul> <li>Positive: Callers catch one stable hierarchy; backend swaps do not ripple into <code>except</code> clauses; credential\u2011safe logging is enforced.</li> <li>Negative: Additional wrapping layer may obscure original tracebacks if not properly chained (mitigated by <code>raise ... from</code>).</li> <li>Neutral: New error subtypes may be added later; major version bumps will only follow removal or renaming.</li> </ul>"},{"location":"adr/0013-error-taxonomy-exception-hierarchy/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Flat list of exceptions with no root Harder to catch \u201cany omniproxy error\u201d in library wrappers. Re\u2011exporting backend exceptions directly Leaks implementation details; breaks when backend changes. Generic <code>Exception</code> in all public APIs Sacrifices granularity needed for retry / circuit\u2011breaker decisions."},{"location":"adr/0013-error-taxonomy-exception-hierarchy/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/exceptions.py</code> (definition of all exception classes)</li> <li><code>omniproxy/backends/</code> (wrapping patterns in each adapter)</li> <li><code>omniproxy/pool.py</code> (<code>PoolCircuitOpenError</code> usage)</li> </ul>"},{"location":"adr/0014-cli-design-integration-principles/","title":"ADR-0014: CLI design and integration principles","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: cli, architecture</li> </ul>"},{"location":"adr/0014-cli-design-integration-principles/#context","title":"Context","text":"<p>The library ships a small CLI (<code>omniproxy check</code>, <code>omniproxy scrape</code>) described as \u201cfor ops\u201d. As the feature set grows, there is pressure to add more commands (watch, scheduled refresh, metrics export). Without a design principle, business logic could leak into the CLI, or the CLI could introduce server\u2011like features that violate ADR\u20110001.</p>"},{"location":"adr/0014-cli-design-integration-principles/#decision","title":"Decision","text":"<ol> <li> <p>The CLI is a thin wrapper: it parses arguments, instantiates objects using the library\u2019s public API, calls library functions, and formats output. No business logic lives inside <code>cli.py</code> or its supporting modules.</p> </li> <li> <p>Every CLI workflow is a library function first: e.g. <code>check_proxies</code> and <code>fetch_proxies</code> are callable from Python; the CLI simply provides argument parsing and output formatting on top.</p> </li> <li> <p>Output formats: the CLI supports plain text (default) and JSON (via a <code>--json</code> flag). Structured output uses <code>orjson</code> (already a hard dependency). No pretty\u2011printer framework is added; the formatting stays minimal.</p> </li> <li> <p>No <code>omniproxy serve</code>: a long\u2011running process that accepts inbound traffic or acts as a daemon is explicitly out of scope, reinforcing ADR\u20110001.</p> </li> <li> <p>CLI\u2011only dependencies (e.g. <code>click</code>, <code>rich</code>) are optional extras that the main library does not import. The CLI may ship as a separate entry point that fails gracefully with an install hint if the extra is missing.</p> </li> </ol>"},{"location":"adr/0014-cli-design-integration-principles/#consequences","title":"Consequences","text":"<ul> <li>Positive: Library stays free of CLI\u2011framework dependencies; all workflows are scriptable without the CLI; scope boundary is enforced.</li> <li>Negative: The CLI stays minimal; users who want rich TUI or daemon features must build their own.</li> <li>Neutral: CLI argument names mirror library parameter names closely, serving as informal documentation.</li> </ul>"},{"location":"adr/0014-cli-design-integration-principles/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Rich, multi\u2011command CLI with progress bars Adds heavy dependencies and encourages business logic in the CLI. No CLI at all Loses quick operational checks and the scraping helper that many users rely on. CLI that duplicates library logic Maintenance fork; bugs fixed in one place but not the other."},{"location":"adr/0014-cli-design-integration-principles/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/cli.py</code> (argparse\u2011based entry points)</li> <li><code>omniproxy/io.py</code> (scrape helpers reused by CLI)</li> <li><code>pyproject.toml</code> (<code>[project.scripts]</code> and optional\u2011dependency <code>cli</code>)</li> </ul>"},{"location":"adr/0015-logging-observability-hooks/","title":"ADR-0015: Logging and observability hooks","text":"<ul> <li>Status: Accepted</li> <li>Date: 2026-07-16</li> <li>Tags: logging, observability</li> </ul>"},{"location":"adr/0015-logging-observability-hooks/#context","title":"Context","text":"<p>Background tasks (health checks, refresh, dead\u2011letter retry, warmup) run without direct caller interaction. When something goes wrong (fetcher returns 500, circuit opens, cooldown evicts a proxy), operators need visibility. The library must emit diagnostic information without imposing a specific logging framework or structured logging dependency.</p>"},{"location":"adr/0015-logging-observability-hooks/#decision","title":"Decision","text":"<ol> <li> <p>Use Python\u2019s standard <code>logging</code> module with a root logger name <code>omniproxy</code>. Child loggers follow module hierarchy (<code>omniproxy.pool</code>, <code>omniproxy.backends</code>, etc.).</p> </li> <li> <p>Log levels by convention:</p> </li> <li><code>DEBUG</code> \u2013 detailed pool events (acquire/release, selection reasoning, health\u2011check probe outcome).</li> <li><code>INFO</code> \u2013 lifecycle transitions (refresh completed, breaker state change, warmup finished).</li> <li><code>WARNING</code> \u2013 recoverable issues (fetcher returned empty, rotation URL failed, dead\u2011letter retry exhausted).</li> <li> <p><code>ERROR</code> \u2013 unexpected internal exceptions (should never happen, but logged for bug reports).</p> </li> <li> <p>Credential safety: log messages that include a <code>Proxy</code> must use <code>proxy.safe_url</code> (or <code>str(proxy.server)</code>). Raw credential strings are never emitted.</p> </li> <li> <p>Structured metrics are deliberately separate from logging. The <code>MetricsExporter</code> protocol (ADR\u20110012) provides a hook for counter/gauge\u2011style metrics. The library does not emit structured metrics via logging (e.g. JSON lines); it uses the metrics hook for that.</p> </li> <li> <p>No logging configuration is applied by the library at import time. Users control handlers, formatters, and levels through their own <code>logging.basicConfig</code> or dict config. The library merely acquires loggers via <code>logging.getLogger(__name__)</code>.</p> </li> </ol>"},{"location":"adr/0015-logging-observability-hooks/#consequences","title":"Consequences","text":"<ul> <li>Positive: Zero\u2011dependency observability; easy to integrate with existing logging setups; safe for credential\u2011heavy environments.</li> <li>Negative: Without structured logging, complex parsing may be needed to extract metrics from text logs (mitigated by the <code>MetricsExporter</code> hook).</li> <li>Neutral: Debug\u2011level logging can be verbose; users may need to adjust levels per logger.</li> </ul>"},{"location":"adr/0015-logging-observability-hooks/#alternatives-considered","title":"Alternatives considered","text":"Alternative Why not Third\u2011party structured logging (structlog, loguru) Forced dependency; not all users want it. Silent by default (no logging at all) Makes operational issues invisible; users insert their own prints. Print\u2011based output Interferes with stdout, no level filtering, not thread\u2011safe."},{"location":"adr/0015-logging-observability-hooks/#evidence","title":"Evidence","text":"<ul> <li><code>omniproxy/utils.py</code> (helper <code>_log_safe</code> or equivalent)</li> <li><code>omniproxy/pool.py</code>, <code>omniproxy/refresh.py</code>, <code>omniproxy/dead_letter.py</code> (logger calls)</li> <li><code>docs/for_mine_and_only_my.md</code> (logging configuration examples)</li> </ul>"}]}